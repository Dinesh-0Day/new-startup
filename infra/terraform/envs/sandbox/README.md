# Sandbox Environment

Local prototyping environment for infrastructure experimentation and developer testing.

Run `terraform init` and `terraform plan` here first; promotes changes to staging after validation.

